if (success||error)
  div(class='alert alert-info alert-block' role='alert' id='loginAlert')
    script.
      $('#loginAlert').slideDown();
    if success
      span() #{success}
    if error
      span() #{error}