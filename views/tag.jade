doctype html
head
  include links.jade
body
  include header.jade

  each post in posts
    li #{post.dates[0].day}
      a(href='/u/#{post.name}/#{post.dates[0].day}/#{post.title}')  #{post.title}
      
  include footer.jade
